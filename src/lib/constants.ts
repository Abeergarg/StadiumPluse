/* ================================================================
   StadiumPulse — constants.ts
   Backward-compatible re-export barrel.
   Import from the specific modules for new code:
     - UI navigation items  → '@/lib/ui-constants'
     - Domain / mock data   → '@/lib/domain-data'
   ================================================================ */
export { NAV_ITEMS, MOB_NAV_ITEMS } from './ui-constants';
export {
  MENU_ITEMS,
  BOT_REPLIES,
  DEMO_CHAT_MSGS,
  DEMO_CHAT_SUGGESTIONS,
  DEMO_CHAT_BOT,
  TICKER_ITEMS,
  ZONE_BADGES,
  FEATURES,
} from './domain-data';
